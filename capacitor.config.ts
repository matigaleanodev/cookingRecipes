import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ar.com.matiasgaleano.foodtracker',
  appName: 'Food Tracker',
  webDir: 'dist/cooking-recipes/browser',
};

export default config;
